Dobra — zrobiłeś już 80% roboty, teraz tylko domykamy spójnie całą logikę i usuwamy stare relikty 👍

⸻

✅ 1. Jak MA działać docelowo (finalny kontrakt)

Parametry wejściowe (WSZYSTKO z CLI, nic z configu):

Masz tylko:

* include_clients (lista albo None)
* exclude_clients (lista albo None)

Interpretacja:

include	exclude	wynik
None	None	✅ wszyscy
[]	None	✅ wszyscy
None	[]	✅ wszyscy
[A,B]	None	✅ tylko A,B
None	[A,B]	✅ wszyscy oprócz A,B
[A,B,C]	[B]	✅ A,C

👉 I to już masz dobrze zrobione w:

resolve_clients(...)

✔️ to jest dokładnie poprawna implementacja
✔️ nic więcej tu nie trzeba zmieniać

⸻

✅ 2. build_dataset_db – jest OK (ale… kilka reliktów)

Twoja funkcja:

build_dataset_db(params, t0, include_clients=None, exclude_clients=None, ...)

✔️ bierze klientów z Excela
✔️ filtruje przez resolve_clients
✔️ działa zgodnie z nową logiką

⸻

❗ Relikty / rzeczy do usunięcia

❌ 1. files_for_client = None

files_for_client = None  # w tym trybie niepotrzebne

👉 do usunięcia (stary tryb multi-file)

⸻

❌ 2. Podwójny print kolejności

Masz 2x:

print("Kolejność przetwarzania...")

👉 zostaw jeden

⸻

❌ 3. komentarze o trybach (myli)

# skip_raw_tables=False, build_derived=True -> NORMAL
# skip_raw_tables=False, build_derived=False -> ETAP 1
# skip_raw_tables=True,  build_derived=True -> ETAP 2

👉 jeśli nie używasz etapów → usuń albo uprość

⸻

❌ 4. params['train_client_id'] – NIE MOŻE ISTNIEĆ

Masz to jeszcze w build_client_db.py (zaraz poprawimy)

⸻

✅ 3. build_client_db.py – TU JEST PROBLEM

Obecnie robisz:

config["first_model_params"]["train_client_id"] = cli_clients

❌ TO MUSI ZNIKNĄĆ

⸻

✔️ POPRAWNA wersja

Dodaj:

parser.add_argument(
    "--include-clients",
    help="Comma-separated list of client IDs to INCLUDE"
)
parser.add_argument(
    "--exclude-clients",
    help="Comma-separated list of client IDs to EXCLUDE"
)

⸻

✔️ parsing

include_clients = parse_client_ids(args.include_clients)
exclude_clients = parse_client_ids(args.exclude_clients)

⸻

✔️ wywołanie

build_dataset_db.build_dataset_db(
    config["first_model_params"],
    t0,
    include_clients=include_clients,
    exclude_clients=exclude_clients
)

⸻

❌ usuń całkowicie:

--build-clients
train_client_id

⸻

✅ FINALNA wersja build_client_db.py (skrót)

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--t0", required=True)
    parser.add_argument("--config", default="config.yaml")
    parser.add_argument("--db-path", required=True)
    parser.add_argument("--include-clients")
    parser.add_argument("--exclude-clients")
    args = parser.parse_args()
    config = load_config(args.config)
    t0 = datetime.strptime(args.t0, "%Y-%m-%d").date()
    config["first_model_params"]["database"] = args.db_path
    include_clients = parse_client_ids(args.include_clients)
    exclude_clients = parse_client_ids(args.exclude_clients)
    print(f"[INFO] include={include_clients}, exclude={exclude_clients}")
    build_dataset_db.build_dataset_db(
        config["first_model_params"],
        t0,
        include_clients=include_clients,
        exclude_clients=exclude_clients
    )

⸻

✅ 4. runner_batch.py – jak to spiąć

Masz teraz:

* --train-client-ids
* --test-client-ids

👉 i to jest OK — tylko:

🔥 WAŻNE: NIE przekazuj tego przez config

Tylko bezpośrednio do:

* build (dataset)
* train
* test

⸻

✅ 5. FINALNE WYWOŁANIA (po zmianach)

⸻

🟢 DB INIT (bez zmian)

python db_init.py ^
  --config config.yaml ^
  --db-path ...

⸻

🟢 BUILD – wszyscy

python build_client_db.py ^
  --t0 2026-03-02 ^
  --db-path ...

⸻

🟢 BUILD – tylko wybrani

python build_client_db.py ^
  --t0 2026-03-02 ^
  --include-clients 13581,10095,6089 ^
  --db-path ...

⸻

🟢 BUILD – wszyscy oprócz

python build_client_db.py ^
  --t0 2026-03-02 ^
  --exclude-clients 13581,10095 ^
  --db-path ...

⸻

🟢 BUILD – include + exclude

python build_client_db.py ^
  --t0 2026-03-02 ^
  --include-clients 13581,10095,6089 ^
  --exclude-clients 10095 ^
  --db-path ...

👉 wynik: 13581, 6089

⸻

✅ TRAIN / TEST (runner_batch.py)

Tu nic nie zmieniasz w CLI — tylko w środku:

⸻

🟢 TEST

python runner_batch.py ^
  --t0 2026-01-01 ^
  --run-test ^
  --test-client-ids 13581,10095,6089 ^
  --db-path ... ^
  --model-path ...

⸻

🟢 TRAIN

python runner_batch.py ^
  --t0 2026-01-01 ^
  --run-train ^
  --train-client-ids 10069,9320,13581 ^
  --db-path ... ^
  --model-path ...

⸻

🟢 TRAIN + TEST

python runner_batch.py ^
  --t0 2026-01-01 ^
  --run-train ^
  --run-test ^
  --train-client-ids 10069,9320,13581 ^
  --test-client-ids 13581,10095,6089 ^
  --db-path ... ^
  --model-path ...

⸻

⚠️ 6. OSTATNIE RELIKTY DO USUNIĘCIA (ważne)

Usuń WSZĘDZIE:

* ❌ train_client_id z configu
* ❌ test_client_id z configu
* ❌ exclude_client_ids z configu

I ich użycia:

params.get("train_client_id")
params.get("test_client_id")

⸻

🧠 TL;DR

✔ Jedno źródło → Excel
✔ Jedna baza
✔ Klienci wybierani WYŁĄCZNIE z CLI
✔ Logika:

final = include or all
final -= exclude

✔ build / train / test → każdy bierze klientów z parametrów
✔ config = tylko techniczne rzeczy (ścieżki, SQL, etc.)

⸻

Jeśli chcesz, mogę teraz:

👉 przejrzeć runner_batch.py i wskazać dokładnie gdzie jeszcze masz stare train/test/exclude z configu (na 99% tam jeszcze coś siedzi)