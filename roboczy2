Problem jest prosty: runner przekazuje listy klientów pod złymi kluczami, a first_model.py nadal czyta je spod starych kluczy.

Dlatego filtr się nie buduje i masz:

[first_model] Client filter: ALL (no filtering)

mimo że podałeś:

--train-client-ids 10069
--test-client-ids 10069

Co obecnie się dzieje

1. runner_batch.py

W main() poprawnie liczysz:

* resolved_train_client_ids = [10069]
* resolved_test_client_ids = [10069]

To jest OK.

2. run_phase() -> prepare_config_for_run()

Tutaj robisz:

cfg["first_model_params"]["client_ids_to_train_on_one_db"] = train_client_ids
cfg["first_model_params"]["client_ids_to_test_on_one_db"] = test_client_ids

ale nie robisz już:

cfg["first_model_params"]["train_client_id"] = train_client_ids
cfg["first_model_params"]["test_client_id"] = test_client_ids

3. first_model.py

A filtr w build_client_filter_sql() czyta właśnie:

ids = params.get('train_client_id')

albo przy teście:

ids = params.get('test_client_id')

czyli szuka starych pól.

W efekcie:

* train_client_id nie istnieje albo jest puste,
* test_client_id nie istnieje albo jest puste,
* więc funkcja zwraca "",
* czyli leci ALL.

To jest główna przyczyna.

⸻

Diagnoza

Masz niespójność między warstwami:

Runner zapisuje

client_ids_to_train_on_one_db
client_ids_to_test_on_one_db

First model odczytuje

train_client_id
test_client_id

To się nie spotyka.

⸻

Co trzeba zmienić

Masz dwie opcje.

Lepsza opcja

Zostawić nowe nazwy pomocnicze, ale dodatkowo ustawiać stare, bo reszta kodu ich jeszcze używa.

Popraw prepare_config_for_run() w runner_batch.py

Zmień na:

def prepare_config_for_run(
    config,
    train_client_ids,
    test_client_ids,
    model_path,
    db_path,
):
    cfg = copy.deepcopy(config)
    cfg["first_model_params"]["database"] = db_path
    cfg["first_model_params"]["test_database"] = db_path
    cfg["first_model_params"]["model_path"] = model_path
    # klucze używane obecnie przez first_model.py
    cfg["first_model_params"]["train_client_id"] = train_client_ids
    cfg["first_model_params"]["test_client_id"] = test_client_ids
    # klucze pomocnicze / nowe
    cfg["first_model_params"]["client_ids_to_train_on_one_db"] = train_client_ids
    cfg["first_model_params"]["client_ids_to_test_on_one_db"] = test_client_ids
    return cfg

To najważniejsza poprawka.

⸻

Dodatkowo

Dobrze też zalogować te wartości przed wywołaniem modelu.

W run_phase() dodaj:

logging.info("train_client_id=%s", cfg["first_model_params"].get("train_client_id"))
logging.info("test_client_id=%s", cfg["first_model_params"].get("test_client_id"))
logging.info("client_ids_to_train_on_one_db=%s", cfg["first_model_params"].get("client_ids_to_train_on_one_db"))
logging.info("client_ids_to_test_on_one_db=%s", cfg["first_model_params"].get("client_ids_to_test_on_one_db"))

Wtedy od razu zobaczysz, co naprawdę trafia do first_model.

⸻

Dlaczego teraz trenuje na wielu klientach

Bo w preprocess_data_model1() dzieje się to:

client_filter_sql = build_client_filter_sql(params, currently_testing=currently_testing)
if client_filter_sql:
    ...
else:
    logging.info("[first_model] Client filter: ALL (no filtering)")

A ponieważ train_client_id / test_client_id nie zostały ustawione w configu przekazywanym do modelu, filtr jest pusty.

Potem:

* base_query idzie bez IN (...),
* liczysz count po całym dataset,
* cap liczysz po wszystkich klientach,
* dlatego pojawia się np. klient 11437.

To właśnie potwierdza, że trening poszedł na full dataset.

⸻

Czy jest coś jeszcze nie tak

Tak, są jeszcze 3 rzeczy do poprawy lub uporządkowania.

1. Literówka w build_dataset_db.py

Masz funkcję:

def build_dataset_db(params, t0, include_clients=None, exclude_clients=None, kip_raw_tables=False, build_derived=True):

a niżej używasz:

if not skip_raw_tables:

Czyli parametr nazywa się kip_raw_tables, a kod używa skip_raw_tables.

To wywali błąd albo będzie działać źle.

Popraw definicję na:

def build_dataset_db(params, t0, include_clients=None, exclude_clients=None, skip_raw_tables=False, build_derived=True):

⸻

2. run_phase() przy train ustawia też test_client_ids

To nie jest błąd krytyczny, ale mylące. W treningu testowa lista nie musi być równa liście testów końcowych. U Ciebie to jest tylko przekazywane dalej i może mieszać w logach albo w plikach wynikowych.

Na teraz może zostać, ale warto wiedzieć.

⸻

3. build_client_db.py

To już wygląda sensownie. Teraz działa zgodnie z nową logiką:

* klienci tylko z CLI,
* include_clients i exclude_clients,
* brak polegania na configowych train_client_id / exclude_ids.

To jest OK.

⸻

Minimalna poprawka, która naprawi Twój problem

Wystarczy ta jedna zmiana w runner_batch.py:

cfg["first_model_params"]["train_client_id"] = train_client_ids
cfg["first_model_params"]["test_client_id"] = test_client_ids

bez tego first_model.py nie ma skąd wziąć filtrów.

⸻

Jak sprawdzić, że już działa

Po poprawce uruchom:

python runner_batch.py ^
  --t0 2026-01-01 ^
  --run-train ^
  --train-client-ids 10069 ^
  --test-client-ids 10069 ^
  --db-path C:/Users/OE00SG/CashPredictor/dev3/databases/2026-03-02/2026-03-02/CashPredictor2.db ^
  --model-path C:/Users/OE00SG/CashPredictor/dev3/model/first_model.pt

I w logu powinieneś zobaczyć coś w tym stylu:

train_client_id=[10069]
test_client_id=[10069]
[first_model] Using client filter: AND INVO_CLNTNO IN (10069)

a nie:

[first_model] Client filter: ALL (no filtering)

I nie powinien się już pojawiać klient 11437 w capach.

⸻

Podsumowanie

Problem

runner_batch.py przestał ustawiać:

* train_client_id
* test_client_id

a first_model.py nadal ich używa do budowania filtra SQL.

Skutek

Model trenuje i testuje na wszystkich klientach.

Rozwiązanie

W prepare_config_for_run() przywrócić:

cfg["first_model_params"]["train_client_id"] = train_client_ids
cfg["first_model_params"]["test_client_id"] = test_client_ids

Dodatkowo popraw

W build_dataset_db.py literówkę:

kip_raw_tables -> skip_raw_tables

Jeśli chcesz, w następnym kroku mogę Ci od razu podać gotowe, finalne wersje runner_batch.py i build_dataset_db.py po tych poprawkach.